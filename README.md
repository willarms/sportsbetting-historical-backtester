# sportsbetting-historical-backtester
Advanced Databases group project

Local testing instructions:
- Make sure dependencies are installed (pip install requirements.txt)
- run backend (venv/bin/uvicorn main:app --reload)
- run frontend in seperate terminal (npm run dev)

Vite (React) app will run on localhost:5173 (default port)
